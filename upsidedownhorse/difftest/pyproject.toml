[project]
name = "difftest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "upsidedownhorse", email = "tp0110@uah.edu" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
difftest = "difftest:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
